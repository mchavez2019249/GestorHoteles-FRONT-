import { FilterHotelPipe } from './filter-hotel.pipe';

describe('FilterHotelPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterHotelPipe();
    expect(pipe).toBeTruthy();
  });
});
